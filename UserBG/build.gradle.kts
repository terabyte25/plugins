version = "1.1.0"
description = "UserBG"

aliucord {
    changelog.set(
        """
        * Added Nitro priority setting - enable to display Nitro instead of UserBG background if user has both
    """.trimIndent()
    )
}